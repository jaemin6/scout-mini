# ----------------------------------------
# 1. ROS 2 작업 공간에서 생성되는 필수 아티팩트 (무조건 제외)
# ----------------------------------------
# colcon/catkin 빌드 아웃풋
build/
install/
log/
.colcon_install_check
*.egg-info/

# ----------------------------------------
# 2. Python 가상 환경 및 캐시 (무조건 제외)
# ----------------------------------------
# 사용자의 myvenv 가상 환경
myvenv/
myvenv_src/
# 기타 일반적인 Python 가상 환경
.venv/
venv/
env/
# Python 캐시 파일
__pycache__/
*.pyc
.mypy_cache/
.pytest_cache/

# ----------------------------------------
# 3. 프로젝트 파일 목록에서 확인된 기타 불필요 파일
# ----------------------------------------
# 이미지 파이프라인/데이터 관련 파일 (소스 코드가 아닌 경우)
# image_pipeline/ (이 폴더 자체를 제외할지 여부는 프로젝트 구조에 따라 다릅니다.
# 보통은 'src' 폴더만 Git에 넣고 빌드 결과는 제외합니다.)

# ----------------------------------------
# 4. OS 및 에디터 임시 파일/설정
# ----------------------------------------
# macOS
.DS_Store

# Linux 및 기타 임시/백업 파일
*~
*.bak
*.swp

# Visual Studio Code 관련 설정 (개인 설정이므로 제외)
.vscode/

# IDE/에디터 관련 임시 폴더
.idea/

# ----------------------------------------
# 5. 대용량 실행 파일 및 데이터 파일 (일반적으로 제외)
# ----------------------------------------
*.bag
*.pcd
*.db
*.sqlite3
maps/

# 이전에 문제가 되었던 폴더들도 명시적으로 추가
rfid_serial_node/
rplidar_ros/
scout_navigation2/maps/
scout_robot/
scout_ros2/